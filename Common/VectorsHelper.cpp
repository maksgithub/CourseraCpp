#include "pch.h"
#include "VectorsHelper.h"
#include <iostream>
#include <vector>

using namespace std;

std::string GetVectorStr(vector<string> vector)
{
    string res;
    /*for (auto value : vector)
    {
        res += value + " ";
    }*/
    return res;
}
